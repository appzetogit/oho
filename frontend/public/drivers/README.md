# Driver photos

Filenames used by the "Meet Our Drivers" section: `driver-1.jpg` … `driver-4.jpg`.

- Square crop, at least 400x400px, face centred.
- Get written consent from each driver before publishing their photo.

Names, ratings, experience and vehicle details live in the `drivers` array in
`src/pages/Home.jsx`.
